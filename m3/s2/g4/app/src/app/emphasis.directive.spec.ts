import { EmphasisDirective } from './emphasis.directive';

describe('EmphasisDirective', () => {
  it('should create an instance', () => {
    const directive = new EmphasisDirective();
    expect(directive).toBeTruthy();
  });
});
